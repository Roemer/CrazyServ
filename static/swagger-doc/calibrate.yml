Calibrates a drone in a swarm.
---
parameters:
  - name: swarm_id
    in: path
    type: string
    description: The id of the swarm.
  - name: drone_id
    in: path
    type: string
    description: The id of the drone.
responses:
  404:
    description: Error if the drone is not found.
  200:
    description: Calibration success status.
    schema:
      $ref: /static/swagger-doc/definitions/success.yml
