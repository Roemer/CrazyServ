Calibrates a drone in a swarm
---
parameters:
  - name: swarm_id
    in: path
    type: string
    description: ID of the swarm
  - name: drone_id
    in: path
    type: string
    description: ID of the drone
responses:
  404:
    description: Error if drone is not found
  200:
    description: Calibration success status
    schema:
      $ref: /static/swagger-doc/success.yml