Reset the package generator of a swarm
---
parameters:
  - name: swarm_id
    in: path
    type: string
    description: ID of the swarm
  - name: seed
    in: query
    type: number
    format: integer
    description: Seed for the pseudo-random number generator
responses:
  200:
    description: Success status of the reset operation
    schema:
      $ref: /static/swagger-doc/success.yml