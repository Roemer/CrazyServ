Gets the status of all drones in a swarm.
---
parameters:
  - name: swarm_id
    in: path
    type: string
    description: The id of the swarm.
responses:
  404:
    description: Error when the swarm with the given id was not found.
  200:
    description: A list of drone stats.
    schema:
      type: array
      items:
        $ref: /static/swagger-doc/drone.yml
