Sends a deliery request for a package
---
parameters:
  - name: swarm_id
    in: path
    type: string
    description: Id of the swarm.
  - name: drone_id
    in: path
    type: string
    description: Id of the drone.
  - name: package_id
    in: query
    type: number
    format: string
    description: Id of the package
responses:
  200:
    description: Success status of the delivery
    schema:
      $ref: /static/swagger-doc/definitions/success.yml