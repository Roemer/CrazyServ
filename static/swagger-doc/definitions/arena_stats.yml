type: object
properties: 
  min_x: 
    type: number
    format: double
    description: X-coordinate (min) of the arena boundary.
  max_x: 
    type: number
    format: double
    description: X-coordinate (max) of the arena boundary.
  min_y: 
    type: number
    format: double
    description: Y-coordinate (min) of the arena boundary.
  max_y: 
    type: number
    format: double
    description: Y-coordinate (max) of the arena boundary.
  min_z: 
    type: number
    format: double
    description: Z-coordinate (min) of the arena boundary.
  max_z: 
    type: number
    format: double
    description: Z-coordinate (max) of the arena boundary.
  buildings: 
    type: array
    items: 
      type: array
      items: 
        type: number
        format: float
    example: [[1, 1, 0], [1, 2, 0]]
    